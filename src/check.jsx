import React from "react";

/* 	export default checkItmes({
        className="check",
        children
    }) => {

      return (
        <div className={`${check} sm:w-full m-2 bg-white shadow p-4 h-full`}>
          {children}
        </div>
      )
    } */